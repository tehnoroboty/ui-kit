import React from 'react';
import { Meta, Title, IconGallery, IconItem } from "@storybook/blocks";
import { icons } from "./iconography";

<Meta title="Design System/Iconography" />

## <h1 style={{margin: '0'}}>Usage</h1>
All the icons in the design system are available as React components. You can use them like this:
```tsx
import { Close } from "@/assets/componentsIcons"

// ...

return <Close />
```

## <h1 style={{margin: '0'}}>Icons</h1>
<IconGallery>
    {Object.keys(icons).map((iconName) => (
        <IconItem key={iconName} name={iconName}>
            <div>{React.createElement(icons[iconName], { width: 24, height: 24, color: '#000' })}</div>
        </IconItem>
    ))}
</IconGallery>